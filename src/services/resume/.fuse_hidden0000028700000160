/**
 * Resume Service Routes
 * The resume service does not expose its own UI pages directly —
 * resume features are embedded within the Profile and Career service UIs.
 * This file is a placeholder to satisfy the SOA routes contract.
 */

export {};
